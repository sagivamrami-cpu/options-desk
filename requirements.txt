# Requires Python >= 3.10 (yfinance -> curl_cffi has no 3.9 wheel).
# Use scripts/bootstrap.sh, which picks a suitable interpreter for you.
yfinance>=1.5
pandas>=2.2
numpy>=1.26
scipy>=1.11
ib_insync>=0.9.86
